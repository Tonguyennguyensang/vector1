#include <iostream>
#include <vetor>
#include <set>
using namespace std;
int main(){
 ios_base::sync_with_stio(0):cin.tie(0);cout.tie(0);
  set<int> s;
  s.insert(10);
  s.insert(9);
  s.insert(1);
  for(auto x : s){
  cout << x <<" "; // tập hợp từ nhỏ đến lớn
} multise t<int> s;
  s.insert(10);
  s.insert(9);
  s.insert(10);
  cout << s.size <<" ";
  for(auto x : s)
  cout << x <<" ";
  
   

}
